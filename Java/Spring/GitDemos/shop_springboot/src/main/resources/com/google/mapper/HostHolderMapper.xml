<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.google.mapper.HostHolderMapper">
    <select id="find" resultType="Consumer">
        select username from online_users where id = #{id}
    </select>
</mapper>